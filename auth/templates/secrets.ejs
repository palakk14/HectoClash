import Image from "next/image"
import Link from "next/link"

export default function HectoClash() {
  return (
    <div className="relative min-h-screen overflow-hidden">
      {/* Background Image */}
      <div className="absolute inset-0 z-0">
        <Image src="/images/background.png" alt="HectoClash Background" fill priority className="object-cover" />
      </div>

      {/* Navigation bar */}
      <nav className="relative z-10 flex items-center justify-center bg-[#1a5b96] p-4 text-white">
        <div className="flex w-full max-w-4xl justify-between px-4">
          <Link href="#profile" className="text-2xl font-bold">
            Profile
          </Link>
          <Link href="#leaderboard" className="text-2xl font-bold">
            LeaderBoard
          </Link>
          <Link href="#points" className="text-2xl font-bold">
            Points
          </Link>
          <Link href="#rulebook" className="text-2xl font-bold">
            RuleBook
          </Link>
        </div>
      </nav>

      {/* Main content */}
      <main className="relative z-10 flex min-h-[calc(100vh-64px)] flex-col items-center justify-start pt-16">
        {/* Welcome text */}
        <div className="mb-8 text-center">
          <h1 className="text-4xl font-bold text-[#0a2e4a] md:text-6xl">WELCOME TO</h1>
          <h1 className="text-6xl font-black text-[#0a2e4a] md:text-8xl">HECTOCLASH !!</h1>
        </div>

        {/* Buttons */}
        <div className="z-10 mt-auto flex flex-col items-center space-y-4 pb-32">
          <button className="w-64 rounded-full bg-gradient-to-r from-[#4a90e2] to-[#1a5b96] py-4 text-3xl font-bold text-white shadow-lg transition-transform hover:scale-105">
            PLAY
          </button>
          <button className="w-64 rounded-full bg-gradient-to-r from-[#4a90e2] to-[#1a5b96] py-4 text-3xl font-bold text-white shadow-lg transition-transform hover:scale-105">
            JOIN
          </button>
          <button className="w-64 rounded-full bg-gradient-to-r from-[#4a90e2] to-[#1a5b96] py-4 text-3xl font-bold text-white shadow-lg transition-transform hover:scale-105">
            SPECTATE
          </button>
          <button className="w-64 rounded-full bg-gradient-to-r from-[#4a90e2] to-[#1a5b96] py-4 text-3xl font-bold text-white shadow-lg transition-transform hover:scale-105">
            CREATE
          </button>
        </div>
      </main>
    </div>
  )
}

